var splide = new Splide( '.splide', {
    type : 'loop',
});
splide.mount();

// 


var splide = new Splide( '.cards', {
    type   : 'loop',
    perPage: 3,
    focus  : 'center',
  } );
  
  splide.mount();
